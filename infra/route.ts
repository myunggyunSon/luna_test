export enum Route {
  INDEX = '/',
  ORDER = '/order',
  ASSIGN = './assign-order',
  SETTLEMENT = './settlement',
  ORDER_LOOKUP = '/order-lookup',
  PAYMENT = './payment'
}

export const INITIAL_ROUTE = Route.INDEX
